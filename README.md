# GPTmain

This repository contains a simple example website with buttons in three different colors:
blue, green, and red.

Open `index.html` in your browser to see the buttons styled via `style.css`.
